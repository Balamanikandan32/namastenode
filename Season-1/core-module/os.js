const os = require("os");

// These are only the some of the os methods.
// Check Node.js documentation for all os methods

console.log(os.platform());

console.log(os.hostname());

console.log(os.version());

// console.log(os.arch());

// console.log(os.cpus());

console.log(os.uptime());
