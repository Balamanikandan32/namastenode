const path = require("path");

// These are only the some of the path methods.
// Check Node.js documentation for all path methods
console.log(path.join("sample", "path"));

console.log(path.resolve("sample"));

console.log(path.dirname("/samplePath/image"));

console.log(path.basename("/sample/path.txt"));
